start :-
    write('Enter first number: '),
    read(A),
    write('Enter second number: '),
    read(B),
    max(A, B, M),
    write('Maximum is: '), writeln(M).

max(X, Y, X) :- X >= Y.
max(X, Y, Y) :- Y > X.
